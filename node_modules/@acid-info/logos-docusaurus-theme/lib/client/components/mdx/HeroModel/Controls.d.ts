import { MovementProps } from '../../../types/ui.types';
import React from 'react';
export declare const Controls: ({ rotateSpeed, enableZoom, enableRotateOnScroll, preset, targetPreset, children, ...props }: MovementProps) => React.JSX.Element;
