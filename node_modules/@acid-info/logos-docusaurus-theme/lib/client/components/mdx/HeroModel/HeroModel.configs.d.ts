import { LookPresetItem } from '../../../types/ui.types';
export declare let ROTATE_SPEED: number;
export declare const MIN_ROTATE_SPEED = 0;
export declare const MAX_ROTATE_SPEED = 1;
export declare const MIN_ZOOM = 0.22;
export declare const RESIZE_SPEED_FACTOR = 0.9;
export declare const INITIAL_ZOOM = 3;
export declare const defaultAsciiConfigs: {
    renderIndex: number;
    bgColor: string;
    fgColor: string;
    characters: string;
    invert: boolean;
    color: boolean;
    resolution: number;
    textShadowSize: number;
};
export declare const defaultPresets: LookPresetItem;
export declare const OBJECTS_PRESETS: LookPresetItem[];
