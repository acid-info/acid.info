import { ECommunityProviders } from '../../types/ui.types';
declare type TProps = {
    handler: string;
    provider: ECommunityProviders;
};
export declare const SocialMediaItem: (props: TProps) => JSX.Element;
export {};
