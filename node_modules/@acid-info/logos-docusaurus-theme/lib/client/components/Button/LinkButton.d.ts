import React, { HTMLProps } from 'react';
declare type TProps = {
    children: React.ReactNode;
    linkProps: HTMLProps<HTMLAnchorElement>;
};
export declare const LinkButton: ({ children, linkProps, ...rest }: TProps & HTMLProps<HTMLButtonElement>) => JSX.Element;
export {};
