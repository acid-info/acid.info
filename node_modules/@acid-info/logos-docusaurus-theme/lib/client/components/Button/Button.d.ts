import React, { HTMLProps } from 'react';
declare type TProps = {
    children: React.ReactNode;
};
export declare const Button: (props: TProps & HTMLProps<HTMLButtonElement>) => JSX.Element;
export {};
