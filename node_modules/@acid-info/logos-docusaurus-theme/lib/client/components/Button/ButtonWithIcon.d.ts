import React, { HTMLProps } from 'react';
declare type TProps = {
    children: React.ReactNode;
    icon: JSX.Element;
};
export declare const ButtonWithIcon: ({ children, icon, ...rest }: TProps & HTMLProps<HTMLButtonElement>) => JSX.Element;
export {};
