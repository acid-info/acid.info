import React, { HTMLProps } from 'react';
declare type TProps = {
    children: React.ReactNode;
    linkProps: HTMLProps<HTMLAnchorElement>;
    icon: JSX.Element;
};
export declare const LinkButtonWithIcon: ({ children, linkProps, icon, ...rest }: TProps & HTMLProps<HTMLButtonElement>) => JSX.Element;
export {};
