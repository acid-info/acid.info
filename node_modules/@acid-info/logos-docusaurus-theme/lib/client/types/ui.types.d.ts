import { GroupProps } from '@react-three/fiber/dist/declarations/src/three-types';
export declare enum ECommunityProviders {
    'discord' = "discord",
    'twitter' = "twitter",
    'linkedin' = "linkedin",
    'status' = "status",
    'github' = "github",
    'discourse' = "discourse",
    'telegram' = "telegram",
    'gscholar' = "gscholar"
}
export declare type LookPresetItemValues = {
    cameraPos: [number, number, number];
    cameraRot: [number, number, number];
    controlsTarget: [number, number, number];
};
export declare type LookPresetItem = {
    modelId: string;
    simple: LookPresetItemValues;
    abstract: LookPresetItemValues;
    targetLook?: LookPresetItemValues;
};
export declare type MovementProps = {
    rotateSpeed?: number;
    enableZoom?: boolean;
    enableRotateOnScroll?: boolean;
    preset: LookPresetItemValues;
    targetPreset?: LookPresetItemValues;
} & GroupProps;
export declare type AsciiConfigs = {
    characters?: string;
    invert?: boolean;
    color?: boolean;
    bgColor?: string;
    fgColor?: string;
    resolution?: number;
    renderIndex?: number;
    textShadowSize?: number;
    withTextShadow?: boolean;
};
