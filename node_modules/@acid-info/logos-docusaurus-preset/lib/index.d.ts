import type { LoadContext, Preset } from '@docusaurus/types';
import { PluginOptions } from './types';
export default function logosPreset(context: LoadContext, options: PluginOptions): Preset;
export * from './types';
export { validateOptions } from './validateOptions';
