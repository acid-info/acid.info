import { ThemeConfig } from '@docusaurus/preset-classic';
import { BusinessUnits } from '../types';
declare const merge: (base: ThemeConfig) => (config: ThemeConfig) => ThemeConfig;
export declare const themeConfigs: Record<BusinessUnits, ReturnType<typeof merge>>;
export {};
