"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.findPluginByName = exports.findPlugin = void 0;
const findPlugin = (plugins, filter) => plugins.filter((plugin) => {
    if (!plugin || typeof plugin === 'function')
        return;
    const [path, config] = Array.isArray(plugin) ? plugin : [plugin, undefined];
    return filter(path, config);
});
exports.findPlugin = findPlugin;
const findPluginByName = (plugins, name) => (0, exports.findPlugin)(plugins, (path) => path.includes(name));
exports.findPluginByName = findPluginByName;
