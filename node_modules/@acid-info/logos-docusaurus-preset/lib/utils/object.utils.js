"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.defaultsDeep = void 0;
const tslib_1 = require("tslib");
const _ = tslib_1.__importStar(require("lodash"));
const defaultsDeep = (objects, mergeArrays = false) => {
    if (mergeArrays)
        return _.defaultsDeep(...objects);
    let output = {};
    objects.reverse().forEach((item) => {
        _.mergeWith(output, item, (objectValue, sourceValue) => {
            return Array.isArray(sourceValue) ? sourceValue : undefined;
        });
    });
    return output;
};
exports.defaultsDeep = defaultsDeep;
