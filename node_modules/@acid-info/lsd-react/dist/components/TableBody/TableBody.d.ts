import React from 'react';
import { CommonProps } from '../../utils/useCommonProps';
import { DropdownOption } from '../Dropdown';
import { tableBodyClasses } from './TableBody.classes';
export type TableBodyProps = CommonProps & Omit<React.HTMLAttributes<HTMLDivElement>, 'buttonLabel'> & {
    options?: DropdownOption[];
    buttonLabel?: 'Button';
    size?: 'small' | 'medium' | 'large';
    toolbar?: React.ReactNode;
};
export declare const TableBody: React.FC<TableBodyProps> & {
    classes: typeof tableBodyClasses;
};
