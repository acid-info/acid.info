import React from 'react';
import { CommonProps } from '../../utils/useCommonProps';
import { DropdownOption } from '../Dropdown';
import { tableClasses } from './Table.classes';
export type TableProps = CommonProps & Omit<React.HTMLAttributes<HTMLDivElement>, 'label'> & {
    size?: 'small' | 'medium' | 'large';
    type?: 'default' | 'checkbox' | 'radio';
    headerOptions?: DropdownOption[];
    header?: React.ReactNode;
    toolbar?: React.ReactNode;
};
export declare const Table: React.FC<TableProps> & {
    classes: typeof tableClasses;
};
